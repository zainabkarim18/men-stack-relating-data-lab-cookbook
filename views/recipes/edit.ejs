<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>

    <form action="/recipes/update/<%=recipe._id%>?_method=PUT" method="POST">
        <div>
            <label for="name">Recipe Name: </label>
            <input type="text" name="name" id="name" value="<%= recipe.name %>" />
        </div>
        <br />

        <div>
            <label for="instructions">Instructions: </label>
            <textarea name="instructions"  id="instructions"><%= recipe.instructions %></textarea>
        </div>

  <label for="ingredients">Ingredients:</label>
      <select id="ingredients" name="ingredients" multiple>
        <% ingredients.forEach(ingredient => { %>
          <option value="<%= ingredient._id %>" <%= recipe.ingredients.some(ing => ing._id.equals(ingredient._id)) ? 'selected' : '' %>><%= ingredient.name %></option>
        <% }) %>
      </select><br>

        <br />

        <button type="submit">Update Recipe</button>
    </form>
</body>
</html>